# Evidence staging area

Historical status snapshots were removed because they described a superseded implementation and obsolete toolchain results. The high-priority consolidation plan is in `docs/ROADMAP.md`.

Until the run-manifest format and retention policy are implemented, do not commit ad hoc “current status” logs here. Link reviews and releases to an exact GitHub Actions run or reproduce the local commands in `docs/VERIFICATION.md`.
