# Architecture decision records

These decisions are proposed and used in the starter, not asserted to be
requirements from MIL-STD-6016. Revisit them with recorded consequences when
actual sources, profiles, or workload evidence require a change.

Current direction-changing decisions:

- [ADR-0009](0009-runtime-schema-packages.md) replaces mandatory build-time
  schema compilation with validated user-supplied schema packages.
