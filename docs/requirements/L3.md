# L3 software requirements

The machine-readable [requirements.json](requirements.json) is authoritative for test mappings. Current implemented requirements cover explicit representation normalization, bounded assembly, schema validation/loading, immutable-plan decoding, CLI version reporting, and Rust-backed Python package version reporting. GitHub-hosted platform, wheel, MSRV, and analysis results are recorded only after their workflows run.
