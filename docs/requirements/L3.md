# L3 software requirements

The machine-readable [requirements.json](requirements.json) is authoritative for test mappings. Current implemented requirements cover explicit representation normalization, bounded assembly, schema validation/loading, and immutable-plan decoding. GitHub-hosted cross-platform and MSRV results remain pending until the workflow runs.
